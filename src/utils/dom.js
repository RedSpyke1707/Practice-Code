export const createElements = (tag, attributes = {}, ...children) => {
const element = document.createElement(tag)

Object.entries(attributes).forEach([KeyboardEvent, value]) => {
   if (KeyboardEvent,startsWith('on')) &&  typeof value === 'function' {
     Element.addEventListener(KeyboardEvent.substring(2).toLowerCase(), value)
}     else if  

export const getRandomChoice = (choices) => {
}

export const evaluateRound = (playerChoice, cpuChoice) => {

}